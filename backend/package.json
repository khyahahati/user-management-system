{
  "name": "user-management-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend scaffold for the Mini User Management System.",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "prisma migrate deploy && node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@prisma/client": "^5.11.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3",
    "prisma": "^5.11.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
